import ViolationsLayout from "@/components/violations/layout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <ViolationsLayout>{children}</ViolationsLayout>;
}
