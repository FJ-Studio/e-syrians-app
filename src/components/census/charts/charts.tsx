import { FC } from "react";
import DailyRegistrations from "./daily-registrations";

const CensusCharts: FC = () => {
  return (
    <div>
      <DailyRegistrations />
    </div>
  );
};

export default CensusCharts;
