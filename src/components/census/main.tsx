import { FC } from "react";

type Props = {
  registrations: number;
};

const CensusMain: FC<Props> = () => {
  return (
    <div>
      <h1>Census Main</h1>
    </div>
  );
};

export default CensusMain;
