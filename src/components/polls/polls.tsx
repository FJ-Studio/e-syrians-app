import { FC } from "react";
import Container from "../shared/container";

const Polls: FC = () => {
  return (
    <Container>
      <div>
        <p>Polls</p>
      </div>
    </Container>
  );
};

export default Polls;
